// Package sdkconfig contains helpers for managing the Go SDK configuration that is used by Relay.
//
// Normally, Relay calls these functions on its own. However, an application that is importing
// Relay as a library can use them to have greater control over its integration with the SDK.
package sdkconfig
